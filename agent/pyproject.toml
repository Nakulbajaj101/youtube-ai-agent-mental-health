[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "elasticsearch>=9.2.1",
    "openai>=2.14.0",
    "pydantic-ai>=1.39.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
